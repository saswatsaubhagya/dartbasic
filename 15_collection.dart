import 'dart:io';

main() {
  //List<int> digits = List(3); //fixed length
  // digits[0] = 1;
  // digits[1] = 2;
  // digits[2] = 3;

  // print(digits);

  // List<String> names = ["a", "b", "c"]; // growable list
  // names.add("d"); // add a single object
  // names.addAll(["e", "f"]);
  // names.add("g");
  // print(names);

  // ///add multiple element
  // print(names);
  // names.remove("a"); //remove specific object object
  // print(names);
  // names.removeLast(); //remove the last element
  // print(names);
  // names.removeAt(2); //remove at location
  // print(names);
  // print(names.length); //length of list

  // Set<int> setlist = Set();
  // setlist.add(1);
  // setlist.add(2);
  // setlist.add(3);
  // setlist.add(4);
  // setlist.add(4); //ignored
  // print(setlist);
  // setlist.length;
  // setlist.remove(1);
  // setlist.forEach((e) {
  //   print(e);
  // });

  // Map<dynamic, dynamic> countryDialingCode = {
  //   // Method 1: Using Literal
  //   "USA": 1,
  //   "INDIA": 91,
  //   "PAKISTAN": 92
  // };

  // Map<String, String> fruits = Map(); // Method 2: Using Constructor
  // fruits["apple"] = "red";
  // fruits["banana"] = "yellow";
  // fruits["guava"] = "green";

  // fruits.containsKey("apple"); // returns true if the KEY is present in Map
  // fruits.update(
  //     "apple", (value) => "green"); // Update the VALUE for the given KEY
  // fruits.remove("apple"); // removes KEY and it's VALUE and returns the VALUE
  // fruits.isEmpty; // returns true if the Map is empty
  // fruits.length; // returns number of elements in Map
  // fruits.clear();// Deletes all elements

  // print(fruits["apple"]);

  // print("\n");

  // for (String key in fruits.keys) {
  //   // Print all keys
  //   print(key);
  // }

  // print("\n");

  // for (String value in fruits.values) {
  //   // Print all values
  //   print(value);
  // }

  // print("\n");

  // fruits.forEach(
  //     (key, value) => print("key: $key and value: $value")); // Using Lambda
}
